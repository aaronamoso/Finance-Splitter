// expense form -- this adds a new expense to the user's expenses list

// PRIORITIZE: THIS mobile version

import React from 'react'

function AddExpense() {
  return (
    <div>
      <form onSubmit="{H"></form>
    </div>
  )
}

export default AddExpense
